<!-- Copyright Kani Contributors -->
<!-- SPDX-License-Identifier: Apache-2.0 OR MIT -->

# Kani `0.67.0+qualified.20260908.1`

Release kind: **unofficial controlled-use GitHub prerelease**

Tag: `qualified-0.67.0+20260908.1`

Profile: [`core-v2`](../../profiles/core-v2.md)

This downstream distribution packages an exact Kani revision carrying the
qualified Rust 1.97.1 alignment, built by the fork's own release workflow, for
conservative, explicitly bounded proof harnesses. It is published from an
unofficial fork and is not sponsored or endorsed by the upstream Kani team.

Kani exists because of substantial work by its maintainers and contributors.
This distribution is grateful for that work, preserves the built artifacts
byte for byte, and is intended to make a narrowly qualified revision available
without implying an upstream release or broader guarantee.

## Exact identity

| Field | Value |
|---|---|
| Distribution version | `0.67.0+qualified.20260908.1` |
| Git tag | `qualified-0.67.0+20260908.1` |
| Tag target | `734fcd7fcf4b1f8b3e37434d911209e6300da643` |
| Source tree | `ee0a58fd8be2e9e13057d7dbe4de588893d949d3` |
| Source repository | `https://github.com/rbeauchamp/kani` |
| Build | [Release Bundle run 34259418299](https://github.com/rbeauchamp/kani/actions/runs/34259418299) |
| Kani-reported version | `Kani Rust Verifier 0.67.0 (734fcd7)` |
| Rust | stable `1.97.1`; commit `8bab26f4f`; LLVM 22.1.6 |
| CBMC | `6.11.0 (cbmc-6.11.0)` |
| Kissat | `4.0.1` |

The Kani executable reports `0.67.0` because the selected development revision
had not changed the package version. The distribution suffix and exact commit
distinguish these artifacts from the official `kani-0.67.0` release.

## Published assets

| Asset | Platform or purpose | SHA-256 |
|---|---|---|
| `kani-0.67.0+qualified.20260908.1-aarch64-apple-darwin.tar.gz` | macOS ARM64 bundle | `0dbd12a6f894fb9a55aa68a9749c4dc4f9da3e3f3d0cbfe7dde69ef2a341c1a6` |
| `kani-0.67.0+qualified.20260908.1-x86_64-unknown-linux-gnu.tar.gz` | Linux x86_64 bundle | `f5be1cbe3d3887e4bea9e8d5a2c856e9ae04a4e5d4425ee1cdef957a9960966a` |
| `kani-verifier-0.67.0-734fcd7.crate` | Exact installer source | `ac1230b2e0b34e55ecb9e8640e76ead5d63b1d0a3fccb74cbcddf087e9169022` |
| `kani-verifier-0.67.0-734fcd7.sbom.json` | Installer SBOM (SPDX 2.3) | `e1261b6ea5f98573f9d7cd14eee28280dad04078d4236be96d06b6eb926b283c` |
| `kani-0.67.0+20260908.1-aarch64-unknown-linux-gnu-not-qualified.tar.gz` | Linux ARM64 bundle — **not qualified** | `2c232b74145c81075ca38746a8a35c3205ca717509270b8ca7207b31f2ee6425` |

The two qualified bundles and installer crate were downloaded from the
successful build. Only their release-facing filenames changed; their contents
and hashes did not. The per-platform `.crate` artifacts of the build are
byte-identical source and are published once.

The `aarch64-unknown-linux-gnu` bundle is published with an explicit
**not-qualified** label, as required by the profile: no public profile gate has
run on that platform. Its filename deliberately omits the `+qualified` marker.
It must not be described or relied upon as qualified.

`SHA256SUMS` covers the bundles, installer source, SBOM, this release-scope
record, and the machine-readable provenance record. `SHA256SUMS.asc` is a
detached signature made by the fork maintainer's Git signing key. The release
tag is also signed. The signing-key fingerprint is
`2A76 8912 80F2 61EB DBC9 1F66 E530 DB27 2392 A04C`; the public key is
available from [`https://github.com/rbeauchamp.gpg`](https://github.com/rbeauchamp.gpg).

## Software bill of materials

`kani-verifier-0.67.0-734fcd7.sbom.json` is an SPDX 2.3 SBOM generated by
cargo-sbom 0.10.0 from the installer crate's locked Rust dependency graph (28
packages; the tool omits the build-script-only helper `cfg_aliases`, a
build-dependency of `nix` that emits cfgs at build time). CBMC, Kissat, the
Charon pin, and the official Rust 1.97.1 toolchain are recorded in
[`PROVENANCE.json`](provenance.json), not in the SBOM.

## Installation

Download the bundle for the target platform, the installer crate, and
`SHA256SUMS` from this release. Verify the files before installation:

```bash
shasum -a 256 -c SHA256SUMS
```

An exact Rust **1.97.1** toolchain for the target is required (for example
`rustup toolchain install 1.97.1`, or the official
`rust-1.97.1-<target>.tar.gz` verified against its published `.sha256`).
Extract and install the exact installer source, then direct it to the
downloaded bundle instead of the official release channel:

```bash
mkdir kani-qualified-installer
tar -xzf kani-verifier-0.67.0-734fcd7.crate -C kani-qualified-installer
cargo +1.97.1 install --locked \
  --path kani-qualified-installer/kani-verifier-0.67.0

cargo kani setup --use-local-bundle \
  ./kani-0.67.0+qualified.20260908.1-<target>.tar.gz

cargo kani --version --verbose
```

Replace `<target>` with `aarch64-apple-darwin` or
`x86_64-unknown-linux-gnu`. Do not run a later plain `cargo kani setup` for this
installation: that command uses the official release channel and may replace
the qualified bundle.

## Qualified scope

This distribution is intended only for the conservative `core-v2` envelope:

- explicitly named proof harnesses;
- exact harness inventory and terminal-result checking;
- reviewed bounded loops with enforced unwind assertions;
- required cover obligations; and
- the pinned default CBMC and Kissat path on the exact stable toolchain tuple
  (rustc/cargo 1.97.1, LLVM 22.1.6, CBMC 6.11.0, kissat 4.0.1).

The complete exclusions remain normative in the
[`core-v2` profile](../../profiles/core-v2.md). Important exclusions include
autoharness, experimental quantifiers, `Rc` or `Arc` nondeterministic
generation, `--quiet`, JSON as a verdict oracle, concurrency claims,
experimental contracts, and any unreviewed warning, unsupported operation,
timeout, `UNDETERMINED`, or unexplained `UNREACHABLE` result.

Each adopting application must retain its own access-controlled manifest and
acceptance receipt. This public distribution does not publish or broaden any
application-specific claim.

## Evidence and limitations

Public evidence includes the exact build and CI results, artifact hashes, tool
identities, the public consumer corpus and its gate receipts on macOS ARM64 and
Linux x86_64, the mutation-detection receipt, the toolchain-equivalence
comparison, the Rust 1.96–1.97.1 compatibility ledger, the fail-closed gate
and its unit tests, and the recorded `core-v2` exclusions.

This release does not establish general Kani, rustc, CBMC, solver, or Rust
correctness. It carries no claim for an excluded feature, an unrecorded target,
or a changed source, toolchain, artifact, flag set, harness set, assumption, or
diagnostic ledger. A change to any of those inputs requires requalification.

The release is marked as a GitHub prerelease because independent public review
of the qualification program remains open. The prerelease is nevertheless
checksum-bound and usable for controlled adopters that satisfy their own
acceptance gate.
